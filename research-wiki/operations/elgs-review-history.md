# EL-GS — Review History (Loop 2, durable record)

Date: 2026-08-08. Reviewer: GPT-5.6-Sol via Codex MCP, xhigh; refine
loop same-thread; adversarial rounds FRESH context each. Raw transcripts
in transient refine-logs/repr-run-2026-08/ notes 13-19; substance here.

## Refine loop (3 rounds, one thread)

R1 5.7 RETHINK: EL-GS v1's evidence coupling was "frozen tracker
pseudo-label regularization"; the tracker-state→existence mapping was
scientifically invalid (occluded flag = learned hypothesis; termination
≠ death; refound ≠ absence-in-between; full censoring makes presence vs
absence-with-return observationally indistinguishable). The reviewer
specified the qualifying principle: RENDERER-CONDITIONED CENSORED
STRUCTURAL INFERENCE (counterfactual observability decides whether
missing evidence is informative; whole-program semi-Markov MAP; the
opposite-decisions-under-identical-dropout diagnostic).
R2 8.2 REVISE ("yes, provisionally" a new inference principle;
provisional calibration "around 8-8.5"): five P0 closures prescribed
(one global energy + Gauss-Seidel coordinate MAP; hypothesis-
marginalized sigma-point observability with LCB negatives and
stop-gradient; evidence conservation under cloning; robust mixture
likelihood + generalized-posterior language; lineage-local late-birth
seeding).
R3 8.9 (→ ~9.1 after final fix): query-source-excluded FAMILY-PRESENT
transmittance (self-occlusion modeled, not removed); conservative
conflict graphs over counterfactual footprints. Approved positioning +
hostile one-liner recorded in [[operations/elgs-method]] provenance.

## Fresh-context adversarial rounds (5) — hostile novelty 4→6→7→7→8

A1 v3 SINKS (4/10): no absent-hypothesis counterfactual (fix: ONE
common evidence-independent bridge family); MAP cannot abstain (fix:
disclosed permanence prior + decision-margin reporting); surrogate/true
energy conflation (fix: two-stage + independent confirmation);
censoring equality must hold identically; decisive β=0-identical-search
control added; dose-matched shifts (r must not vacuously disable).
A2 v4 SINKS (6/10): "identifiable from data" provably false for
reactivate-vs-rebirth equivalences (→ conditional-selection claim,
stratified linkage reporting); existential bridge-family likelihood
undefined for positive evidence (→ composite score); frozen-exogenous-q
identified as THE novelty contrast (→ 5-arm q matrix); confirmation-
sample accounting; capacity-matched return control; likelihood algebra
bug fixed (L0 includes outlier mass).
A3 v5 SINKS (7/10): composite score still not a unique scalar; e-process
language unsupported (→ honest heuristic demotion, validated by
shifted-lane false-event rates); 3-way return ops not closed (→
admissibility with mutually exclusive REACTIVATE/MERGE preconditions);
binding re-audit AFTER round 1 invalidates tested identities (→ moved
to 2.8k, then permanent); prior-driven label 3-way; camera-fold
cross-fitting.
A4 v6 SINKS (7/10): as A3's disposition, plus family/cluster/merge
factor contradictions (→ cluster = connected components of seed-overlap
graph; account = cluster set), feasibility worst-case tables, schedule
softening, ghost-metric renamed lineage-removal sensitivity.
A5 v7 SINKS (8/10) — "the episodic family representation is strong and
plausibly new on the accepted boundary, but its centerpiece structural
energy and claimed invariances are not yet mathematically
well-defined." Remaining fatal = formal write-out only; reviewer
supplied exact forms (tempered-mixture A_B; likelihood-RATIO factors
zeroing censored segments across structures; one bridge latent per
decision; ε-bound with density floors). Adopted as the v8 fix set
([[operations/elgs-method]]).

## Calibrated novelty verdict

[[operations/elgs-novelty-record]]: **8.0/10 PROCEED WITH CAUTION**,
conditional on the v8 write-out surviving one further fresh round;
CC2 renderer-conditioned censored evidence HIGH; CC4 own-content
reactivation HIGH; CC5 conjunction HIGH; fall-back 6.5-7.0 if q
degrades to a confidence weight.

## Rejected Loop-2 formulations (ledger addendum)

- EL-GS v1 evidence mapping (track-alive⇒presence; termination⇒death):
  FUNDAMENTAL as formulated (invalid measurement semantics).
- "Measured existence" framing: rejected (claim inflation).
- Per-scalar description-cost 'one criterion' rhetoric and e-process
  validity claims: rejected; only the heuristic acceptance +
  empirical stress-testing survives.
- Per-segment bridge selection: rejected (chimera evidence).

## Cross-cutting conclusion

Loop 2 reproduced both project lessons at larger scale: (i) claim
inflation (measurement, identifiability, statistical validity) was the
fatal pattern every round — the surviving form is strictly conditional;
(ii) fresh-context adversarial review remains the binding standard —
the same-thread 8.9 preceded five fresh SINKS. New lesson: hostile
rounds converged monotonically (4→8) once each round's demands were
adopted verbatim rather than argued with; the final gate is now
mathematical composition, not design.

## Gate rounds on the formal specification (2026-08-09)

User approval received: complete the v8 write-out, run the final
adversarial round, pass ⇒ implementation-ready.
G1 (v8.0 → thread 019fe378-cf80-...): SINKS. Found: ε-bound false in
the negative direction (correct two-sided M(ε) supplied); PROP 2 false
unconditionally; PROP 1/3 missing tie-break/ownership conditions;
chain parameterization incompatible with the bounded final endpoint;
q not guaranteed ∈[0,1]; history charges not a state energy (ledger
required); cap-operator/censored-reference semantics; SNIS-vs-exact
language; hash-partition collision claim. Novelty 8/10 held;
"repairable specification defects rather than evidence the
representation idea is unsound."
G2 (v8.1 → thread 019fe382-fde9-...): SINKS. Validated: likelihood
algebra, two-sided ε-bound, PROP 1 (conditional), inactive-clipping.
Found: softmax simplex cannot represent exact boundary/latched states
(the substantive defect); family-to-evidence map missing; snapshot vs
candidate-dependent q; per-case transition deltas; estimator
conventions. Novelty 8/10 held.
G3 (v8.2 → thread 019fe38c-dd6f-...): SINKS with exactly THREE
category-A defects (latched-span budget definition; single-valued
cluster binding via a canonical point; MERGE survivor identity), all
else validated under the A/B/C rubric (A = material divergence between
faithful implementations; B = preregistration conventions; C =
empirical risks).
G3-closure (same thread, v8.3): all three repairs verified closed, no
new category-A defects — **SURVIVES-WITH-RISKS, hostile novelty 8/10:
"the exact episodic latch representation, ownership-controlled censored
evidence, and transaction-aware structural search form a distinctive
and now implementable construction."** Category-B items (tie orders,
derived p_floor/p_cap, latch-transition tables, bootstrap mechanics,
thresholds) must be committed as preregistration tables with the
implementation; category-C risks are exactly what the preregistered
plan tests.

GATE STATUS: **PASSED 2026-08-09.** The calibrated 8.0 condition is
satisfied; EL-GS is implementation-ready under
[[operations/elgs-experiment-plan]].

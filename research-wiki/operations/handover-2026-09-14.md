---
title: "HANDOVER 2026-09-14 — wave 2 of 'Occlusion is not absence': stage 1 done, stage 2 waiting on four decisions; how to resume"
date: 2026-09-14
evidence_bearing: false
---

# Handover (2026-09-14) — resume here

Read this page, then [[absfix-wave2-spec-v2-2026-09-11]] §11 (binding
rules) and §13.9–13.20 (every decision, correction and hash since the
user approved the spec), then [[absfix-wave2-stage1-2026-09-13]]. The
JSON `configs/n3v/absfix_gate_spec_v2.json` mirrors the page
(`FREEZE_LIST_STATUS`, `S2`, `VOTE_SEED_BOXES`, `PLATE_CENSUS`,
`EXTERNAL_BASELINE`). The machine record of stage 1 is
`research-wiki/assets/absfix-stage1-record.json` (table:
`absfix-stage1-record.md`; recount: `absfix-box-recount.md`; provisional
L draws: `absfix-draws-v3prov.json`). Repository HEAD at handover: the
commit carrying this page; Leonardo's checkout is fast-forwarded to it.

## 1. Where the paper stands

Thesis and evidence: [[paper-thesis-v1-2026-09-11]] (four claims, §8
amendments), reviewed by [[paper-thesis-v1-kill-argument-2026-09-11]]
(FAIL, four unresolved points, each disposed into spec v2.0.0). Closest
work and datasets: [[literature-check-absence-return-2026-09-10]]
(PersistGS first). Fixtures: `_v3` derived roots of flame_steak and
sear_steak (cap removed; hull box disabled, §13.6–13.7),
cut_roasted_beef wave-1 root (calibration). Plate census NEGATIVE on all
three scenes (§13.12): the insertion fixture is the v3 candidate (a new
frozen spec with its own kill-argument), not started.

## 2. Stage 1 is terminal; what it established

Eight prefixes, eight T1 estimates, eight construction-derived votes
(re-seeded from authored per-scene boxes, §13.15–13.16), A/B shams on
every prefix, SAM2 masks with one bounded re-seed (§13.10–13.14), S2
votes and preconditions where the instrument is admitted, the recount of
every program against the frozen setup boxes (§13.19). Membership
precondition PASSES on flame_steak 4/4 and calibration 4/4 (precision
≥ 0.9996, recall 0.978–0.990). Under §11.5 as frozen, Claim B is
DESIGN_WITHOUT_POWER on both confirmatory scenes (sear_steak: the S2
vote refuses on cam09's countertop mask; flame_steak: prefix 3 fails the
T1 bound); Claim A is exercisable on both.

## 3. The four decisions stage 2 waits for (the user's, not an agent's)

1. **The L sham's measure and rule** (§13.19–13.20). The frozen measure
   (`w_in`, contribution inside the object silhouette) makes any
   mass-matched sham impossible: the non-truth cloud carries 0.21–0.48 of
   the truth mass. On `w_total` (total paint in the view) the ORIGINAL
   local rule is constructible on 12/12 prefixes (local mass 0.95–1.60 ×
   truth) and the radius rule needs 0.71–0.74 units (provisional draws in
   `draws_prefix<S>_v3prov`, job 57559108). Recommended: keep the local
   rule, change the measure to `w_total`. Once decided: amend §11.2
   append-only, draw L on all 12 prefixes with
   `scripts/draw_membership_shams.py --contribution_key w_total --l_mode
   local` (or `radius --xyz`), hash, add to the freeze list.
2. **A/B exemption from the in-box clause of §11.4** (§13.19): the
   count-matched uniform shams place 46–103 rows in the box (23 of 24
   cells below 100) because they are non-local by definition.
   Recommended: exempt A and B from the in-box clause only.
3. **Accept Claim B as DWP on both confirmatory scenes** (or amend the
   camera-drop rule of §13.9(b), which would be post hoc). GESTMEM arms
   are still trained where admitted and reported descriptively.
4. **Stage-2 go**: 72 continuations + 16 calibration additions + the
   STG baseline, ≈ 250–300 A100-h, 2–3 days of wall time with a warden.

## 4. How to resume (exact)

* Leonardo: `W=/leonardo_work/EUHPC_D36_068/sri/proj_adags`; repo
  `$W/repo/adags` (fast-forward to origin first); ledger
  `$W/agent-control/realdata/jobs/ledger.txt` (append-only, every job id
  with a reason); stage-1 chain files and sbatch templates under
  `$W/agent-control/realdata/absfix2/stage1/` (`chain*.txt`,
  `jid_*.txt`, `seed_boxes.json`, `vote.sbatch`, `s2_vote.sbatch`,
  `draws*.sbatch`, `precond*.sbatch`, `t1.sbatch`, `prefix_cell.sbatch`);
  outputs `$W/runs/realdata/absfix2/<scene>/{prefixes, t1_prefix<S>,
  votes_prefix<S>_seedbox, draws_prefix<S>_v2|_v3prov, s2vote_prefix<S>_seedbox,
  precond_prefix<S>_seedbox, box_recount_prefix<S>.json}`; SAM2 masks
  `$W/runs/realdata/s2/<scene>/final/`; STG `$W/repo/stg`, `$W/envs/stg`,
  `$W/agent-control/realdata/stg/README_stage2.md`.
* Windows: SSH through PowerShell or, from Bash, the native binary by
  full path (`/c/Windows/System32/OpenSSH/ssh.exe`); the CINECA
  certificate lasts 12 h and only the user renews it. Never glob the
  `logs/` directory; use exact `<jobname>_<jobid>.out` names.
* Stage-2 continuations: per scene and prefix, nine arms (§11.2) as
  continuations of the prefix's `chkpnt6000.pth` to 12k with the program
  JSON of the arm; configs and traps in [[minimal-path-config-2026-09-11]]
  (reserved parity; score ONLY with `scripts/eval_n3v_gated.py
  --restore_state`); reducer `scripts/realdata_gate_analysis.py --spec
  configs/n3v/absfix_gate_spec_v2.json --paired` (v2 keys); run a warden:
  a job whose `.out` stays at 0 bytes longer than its siblings' whole
  runtime is hung (start-up stall; node lrdn0136 twice), cancel and
  resubmit identically.
* Operating rules that bit this block: compare BUILD composites, never
  preview against build; a vote must not be seeded from the estimator
  whose failure it must survive; preconditions that do not depend on
  each other must be separate jobs; a mass-matching measure defined by
  the object region cannot be matched by non-object rows.

## 5. Notes from the user (2026-09-14)

* SAM 3 (github.com/facebookresearch/sam3, huggingface facebook/sam3)
  could replace SAM 2 as the S2 segmenter. Not for wave 2 (S2 is frozen
  at the SAM 2.1 checkpoint of §13.8); a candidate for the v3 fixture's
  instrument, to be pinned the same way (checkpoint sha256, config,
  commit, click rule) if adopted.
* Research structuring principle stated by the user: go from the
  simplest implementable idea where the method can be shown to produce
  excellent results, then increase complexity until it is tested on
  real-world data and several datasets. The current ladder already
  follows it (LRV3 synthetic → authored absence on real footage →
  insertion fixture → real events), and it should govern the v3 spec.
